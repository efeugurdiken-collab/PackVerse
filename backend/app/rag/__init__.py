"""RAG (Retrieval-Augmented Generation) utilities (Sprint P10B1 onward).

Currently just deterministic text chunking (app/rag/chunking.py). No
ingestion, embedding, or retrieval code lives here yet - see that
module's docstring for exact scope.
"""
