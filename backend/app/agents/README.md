# agents

Runtime implementations of the Agents specified in the PackVerse OS
Obsidian vault (`05 Agents/`). Intentionally empty in Sprint P1 -
this sprint is infrastructure only, no AI features, no Agents yet.
Populated starting with Sprint P6 (AI Runtime) per the roadmap.
