# workflows

Runtime implementations of the Workflows specified in the PackVerse OS
Obsidian vault (`06 Workflows/`). Intentionally empty in Sprint P1.
Populated starting with Sprint P9 (Product Factory) per the roadmap.
